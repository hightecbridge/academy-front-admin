/**
 * 공통 API 게이트웨이(servlet context `/api`) 아래 학원(Hi Academy) 서비스 접두사.
 * 백엔드 `ServicePaths.ACADEMY` 및 axios `baseURL`과 반드시 동일해야 함.
 */
export const ACADEMY_API_BASE_PATH = 'https://api.dbridgehub.com/api/academy'
